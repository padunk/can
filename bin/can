#!/usr/bin/env node
const path = require('path');

require = require('esm')(module /*, options */);
// require('../src/cli.js').cli(process.argv);
require(path.resolve(process.cwd(), 'src/cli.js')).cli(process.argv);
